/** @format */

import { Spinner } from '@chakra-ui/react';

const PlatformSpinner = () => {
  return <Spinner size='sm' position='absolute' right='10px' top='20%' transform='translateY(-50%)' zIndex='1' />;
};

export default PlatformSpinner;
