/** @format */

import { Button, HStack } from '@chakra-ui/react';

const App = () => {
  return (
    <HStack>
      <Button colorPalette='gray'>Click me</Button>
      <Button colorPalette='red'>Click me</Button>
    </HStack>
  );
};
export default App;
